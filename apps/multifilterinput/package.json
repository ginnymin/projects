{
  "name": "multifilterinput",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "tsc -b && eslint .",
    "preview": "vite preview",
    "test": "vitest run"
  }
}
